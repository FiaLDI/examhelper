
type QuestionCardProps = {
  title: string;
  description: string;
  number?: string;
  remove?: () => void;
  edit?: () => void;
  link?: () => void;
};

export const QuestionCard = ({}: QuestionCardProps) => {

    return (
        <div>

            1234343
        </div>
    )
}